Assignment 1: HTML & CSS Basics

Student Name: Daniyar Abdusadykov  
Group: IT-2504  
Live Site URL:** https://your-username.github.io/assignment1-frontend/  



 Objective
To understand the core concepts of HTML and CSS, structure web pages using basic and intermediate HTML tags, apply styling via inline, internal, and external CSS, understand layout mechanisms (Box Model, Float, Positioning), and publish the website via GitHub Pages.



 Tasks & Implementation Screenshots
Part 1 & Part 2: HTML Structure, Lists, Tables & Forms
 Created standard boilerplate HTML5 structure.
 Structured headings (`<h1>`-`<h3>`), paragraphs, ordered (`<ol>`) and unordered (`<ul>`) lists.
 Added images, hyperlink navigation, buttons, emojis, contact form, and a weekly class schedule table.

![HTML Structure & Forms Screenshot](screenshots/part1_2.png)



 Part 3 & Part 4: CSS Styling, Box Model, Layout & Positioning
 Linked external CSS (`style.css`), internal styles in `<head>`, and inline styling.
 Applied element, class (`.highlight`), and ID (`#main-heading`) selectors.
 Configured CSS Box Model (padding, margins, borders) and sizing units (`px`, `%`, `em`, `rem`).
 Implemented `float` & `clear` layouts and demonstrated `static` vs `relative` positioning.

![CSS Styling Screenshot](screenshots/part3_4.png)



 Summary of Work Process
During this assignment, I systematically created a personal profile webpage using HTML5 and CSS3. I started by structuring the raw content with semantic HTML elements (headers, lists, sections, tables, and forms). Next, I integrated CSS styles using different methods, learning the specificity hierarchy between inline, internal, and external styles. Finally, I experimented with modern box model properties, units, and positioning mechanisms to achieve a clean layout. The project was successfully deployed to GitHub Pages.
